export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api'
};

// sirve para tener un lugar centralizado donde guardar las configuraciones globales
// de  la aplicacion, nos sirve para no escribir la url multiples veces en el codigo,
